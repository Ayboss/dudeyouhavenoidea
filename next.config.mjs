/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "dudeyouhavenoidea.hashnode.dev",
      "plus.unsplash.com",
      "cdn.hashnode.com",
    ],
  },
};

export default nextConfig;
